<%= simple_form_for(@fuel_tank) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <div class="row justify-content-center">
      <div class="col-lg-6">
        <%= f.input :capacity, label: 'Capacidade', required: true, autofocus: true %>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-lg-6">
        <%= f.input :amount, label: 'Quantidade', required: true %>
      </div>
    </div>
    <div class="row justify-content-center">
      <div class="col-lg-6">
        <%= f.input :fuel, label: 'Combustível', collection: FuelTank.fuels.keys %>
      </div>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit, locals[:button_label], { class: 'btn btn-primary' } %>
  </div>
<% end %>